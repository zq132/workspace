1、libecportcheck.so 为程序运行所需动态库，若直接运行失败可将其放在lib或/usr/lib目录下
2、register.txt  运行所需注册码文件，需放在当前目录且文件名称类型一致。
3、*.h *.cpp  程序源文件
4、build  程序编译产生的中间代码
5、test  可执行文件(测试动态库功能所用)

运行程序时，判断注册码文件是否存在，及注册码是否正确，若正确则监听目标端口8800，有信息发出时退出程序，程序运行时没有任何输出（注：一般个人电脑不会有8800端口数据，所以程序会一直运行且没有输出）
